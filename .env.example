# EasyCLIAgent 환경변수 예시
# 실제 사용시 .env 파일로 복사하여 사용하세요

# ===========================================
# 개발 환경 설정
# ===========================================
NODE_ENV=development
VITE_DEV_SERVER_URL=http://localhost:5173

# ===========================================
# 앱 설정
# ===========================================
VITE_APP_VERSION=1.0.0
VITE_APP_NAME=EasyCLIAgent

# ===========================================
# AI CLI 도구 설정 (선택사항)
# ===========================================
# Gemini CLI가 설치된 경로 (기본값: 글로벌 설치)
# GEMINI_CLI_PATH=gemini

# Claude Code가 설치된 경로 (기본값: 글로벌 설치)
# CLAUDE_CLI_PATH=claude

# ===========================================
# 로깅 설정
# ===========================================
LOG_LEVEL=info
ENABLE_DEBUG_LOGS=false

# ===========================================
# 보안 설정 (Electron)
# ===========================================
ENABLE_SANDBOX=true
DISABLE_NODE_INTEGRATION=true

# ===========================================
# 개발자 도구 설정
# ===========================================
# 프로덕션에서 개발자 도구 활성화 (기본값: false)
ENABLE_DEVTOOLS_IN_PRODUCTION=false

# ===========================================
# 파일 감시 설정
# ===========================================
# 파일 시스템 감시 활성화 (기본값: true)
ENABLE_FILE_WATCHING=true

# 감시에서 제외할 패턴 (쉼표로 구분)
FILE_WATCH_IGNORE_PATTERNS=node_modules,dist,build,.git

# ===========================================
# 터미널 설정
# ===========================================
# 기본 터미널 셸 (시스템 기본값 사용시 비워둠)
# DEFAULT_SHELL=/bin/zsh

# 터미널 컬러 지원 활성화
ENABLE_TERMINAL_COLORS=true